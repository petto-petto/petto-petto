# petto-petto 디자인 가이드

## 1. 디자인 방향

petto-petto는 어두운 숲속의 작은 펫 공간을 닮은 픽셀 UI다. 짙은 숲 배경 위에 나무와 양피지 질감의 패널을 배치하고, 펫과 조작 요소를 선명한 정수 픽셀로 표현한다.

- 둥근 카드, 유리 효과, 흐린 그림자는 사용하지 않는다.
- 테두리와 그림자는 선명한 픽셀 단위로 구성한다.
- 정보는 차분한 양피지 패널에, 조작은 나무 버튼에, 성취와 희귀도는 금색 및 희귀도 색상으로 표현한다.
- 배경은 조용하게 유지하여 펫, 선택 상태, 진행 상태가 가장 먼저 눈에 들어오게 한다.

## 2. 색상

### 기본 토큰

| 역할 | 값 |
| --- | --- |
| 가장 어두운 숲 배경 | `#10231A` |
| 중간 숲색 | `#1C4A34` |
| 밝은 잎색 | `#3C7A4A` |
| 숲의 빛 | `#8FD68A` |
| 나무 테두리 | `#3A2A1C` |
| 나무 표면 | `#6B4A2E` |
| 나무 하이라이트 | `#A5763F` |
| 양피지 표면 | `#F3E3C0` |
| 양피지 안쪽 링 | `#E0C894` |
| 밝은 패널 위 글자 | `#3B2A1A` |
| 어두운 표면 위 글자 | `#F5ECD8` |
| 금색 강조 | `#FFD166` |
| 어두운 외곽선 | `#2C2438` |

### 상태와 희귀도

| 상태 | 밝은 색 | 어두운 바탕 |
| --- | --- | --- |
| COMMON | `#9AA0A6` | `#62676C` |
| RARE | `#4AA3FF` | `#1F5A99` |
| EPIC | `#B46BFF` | `#6C37A0` |
| 경고 | `#F0B775` | `#725127` |
| 오류 | `#F08A8A` | `#762F35` |
| 비활성 | `#999999` | `#555555` |

- 희귀도 색상은 희귀도를 알리는 용도로만 사용한다. 희귀도 배지, 도감 슬롯의 안쪽 테두리, 펫 희귀도 아이콘에 적용한다.
- 금색은 현재 선택, 달성, 보상처럼 즉시 주의가 필요한 상태에만 사용한다.
- 경고와 오류는 색상만으로 구분하지 않고 아이콘 또는 텍스트를 함께 제공한다.

## 3. 타이포그래피

- 본문에는 `Galmuri9`, 제목과 희귀도 라벨에는 굵은 `Galmuri11`을 사용한다.
- 폴백 글꼴은 `ui-monospace, monospace` 순서로 둔다.
- 글꼴 크기는 `10 / 11 / 12 / 13 / 15 / 16px`만 사용한다.

| 용도 | 크기 | 글꼴 |
| --- | --- | --- |
| 보조 정보와 숫자 라벨 | 10px | 본문 |
| 희귀도 배지 | 11px | 제목 |
| 본문, 버튼, 진행도 | 12px | 본문 |
| 화면 라벨 | 13px | 제목 |
| 패널 제목 | 15px | 제목 |
| 화면 제목 | 16px | 제목 |

- 픽셀 글꼴의 외곽을 부드럽게 처리하지 않는다. `-webkit-font-smoothing: none`과 `text-rendering: optimizeSpeed`를 적용한다.
- 본문은 짧고 값 중심으로 작성하며, 한 줄에 과도한 정보를 넣지 않는다.
- 어두운 표면에는 밝은 글자, 양피지 표면에는 잉크색 글자를 사용한다.

## 4. 픽셀 렌더링

- 스프라이트, 픽셀 배경, 픽셀 아이콘은 정수 배율로만 확대한다.
- 이미지에는 `image-rendering: pixelated`를 적용하고, Canvas 렌더링에는 `imageSmoothingEnabled = false`를 설정한다.
- 펫 스프라이트의 공통 외곽선 색상은 `#2C2438`이다.
- 광원은 좌상단에 둔다. 하이라이트는 좌상단, 그림자는 우하단에 배치한다.
- 펫의 픽셀은 완전 투명 또는 완전 불투명만 사용한다.

## 5. 형태와 간격

### 간격

기본 단위는 2px이며, 레이아웃에는 아래 값을 사용한다.

| 이름 | 값 | 용도 |
| --- | --- | --- |
| `space-1` | 4px | 아이콘과 짧은 라벨 사이 |
| `space-2` | 6px | 작은 항목 사이 |
| `space-3` | 8px | 버튼 안쪽 여백 |
| `space-4` | 10px | 관련 정보 묶음 사이 |
| `space-5` | 12px | 카드와 그리드 간격 |
| `space-6` | 14px | 패널 안쪽 여백 |
| `space-7` | 18px | 화면 가장자리 여백 |

### 테두리와 그림자

- 일반 UI 요소에는 `border-radius`를 사용하지 않는다.
- 외부 드롭 섀도우는 블러 없는 `2px 2px 0` 오프셋으로 표현한다. 안쪽 하이라이트와 음영은 베벨 표현에만 사용한다.
- 양피지 패널에는 3px 나무 테두리, 2px 나무 하이라이트, 5px 양피지 안쪽 링을 사용한다.
- 나무 버튼에는 2px 나무 테두리와 밝은 위쪽 면, 어두운 아래쪽 면을 적용해 깊이를 만든다.

```css
.pixel-panel {
  border: 3px solid #3A2A1C;
  box-shadow:
    inset 0 0 0 2px #A5763F,
    inset 0 0 0 5px #E0C894,
    2px 2px 0 rgba(0, 0, 0, 0.35);
}

.pixel-button {
  border: 2px solid #3A2A1C;
  box-shadow:
    inset 0 2px 0 #A5763F,
    inset 0 -2px 0 #3A2A1C,
    2px 2px 0 rgba(0, 0, 0, 0.35);
}
```

## 6. 컴포넌트

### 패널

- 정보 패널의 기본 표면에는 양피지를 사용한다.
- 패널 제목은 좌상단에 배치하고, 제목과 본문 사이에는 8~14px 간격을 둔다.
- 고밀도 정보는 제목, 핵심 값, 보조 정보의 세 단계로 구성한다.

### 버튼

- 주요 버튼에는 나무 표면과 밝은 글자를 사용한다.
- 비활성 버튼은 회색 표면과 낮은 대비의 글자로 표현하며, 눌림 반응을 제공하지 않는다.
- 누를 때 버튼을 `translate(2px, 2px)`만큼 이동하고 드롭 섀도우를 제거한다.
- 아이콘 전용 버튼에는 보이는 텍스트 라벨 또는 접근 가능한 이름을 제공한다.

### 배지와 진행도

- 희귀도 배지는 어두운 희귀도 색상 바탕, 밝은 희귀도 색상 테두리, `#F5ECD8` 글자로 구성한다.
- XP 바는 높이 10px, 테두리 2px을 사용한다.
- XP 바의 채움은 위에서 아래로 `#8FD68A`, `#3C7A4A`, `#1C4A34` 순서로 변화시킨다.
- 진행도 색상에는 숫자 또는 짧은 상태 라벨을 함께 제공한다.

### 펫과 선택 상태

- 펫은 중앙 행동 영역에 두고, 주변 패널보다 먼저 주목받을 수 있도록 충분한 빈 공간을 둔다.
- 현재 선택에는 2px 금색 점선 테두리를 사용한다.
- 펫 외곽선이나 중요한 정보를 가리지 않도록 작은 타원형 바닥 그림자를 사용한다.

## 7. 화면 레이아웃

### 펫룸

- 배경은 깊은 숲, 중경의 잎, 바닥 공터의 세 층으로 구성한다.
- 펫의 공터는 화면 중앙 하단에 배치하고, 주변보다 약간 밝게 표현한다.
- 상세 패널은 펫의 움직임이나 상태를 가리지 않는 화면 가장자리에 배치한다.
- 조작 요소는 한쪽에 모아 펫의 활동 공간을 보존한다.

### 도감

- 도감 항목은 같은 크기의 정사각형 슬롯 그리드로 배치한다.
- 슬롯 사이 간격은 12px으로 유지한다.
- 획득한 펫에는 스프라이트와 희귀도 색상을, 미획득 펫에는 낮은 대비의 빈 슬롯을 사용한다.
- 선택은 금색 바깥 테두리로, 희귀도는 희귀도 색상 안쪽 테두리와 배지로 표시한다. 배경 장식이 이 상태 표현과 경쟁하지 않게 한다.

## 8. 모션과 접근성

- 모션은 상태 변화에 대한 짧은 피드백에만 사용한다.
- 버튼 눌림은 위치 변화로 전달하고, 새 펫 또는 보상에는 작은 픽셀 단위의 팝 효과를 사용한다.
- 장식용 반복 애니메이션은 피하며, `prefers-reduced-motion` 환경에서는 움직임을 줄이거나 제거한다.
- 키보드 포커스는 2px 금색 외곽선과 바깥쪽 1px `#2C2438` 보조 링으로 명확히 보이게 한다.
- 작은 픽셀 글꼴에서도 텍스트와 배경 사이에 충분한 대비를 유지한다.
- 색상에 의존하는 상태는 아이콘, 텍스트, 패턴 중 하나를 함께 제공한다.
