# 공통 베이스 — 결정해야 할 것

이 문서는 답이 아니라 **안건**이다. `meta` 프로토타입을 만들면서 "이건 나 혼자 정하면
안 되겠다" 싶었던 것들을 모았다. 각 항목은 선택지와 그 선택이 불러올 결과만 적는다.

구조 제안과 그 근거는 [`ARCHITECTURE.md`](ARCHITECTURE.md)에 있다. 그쪽은 프로토타입이
이미 채택한 것이고, 이 문서는 **아직 비어 있는 자리**다.

> TypeScript 용어가 낯설면 [`ARCHITECTURE.md`의 "Java로 읽는 TypeScript 용어"](ARCHITECTURE.md#java로-읽는-typescript-용어)
> 대응표를 먼저 보면 된다. 이 문서에서 자주 나오는 것만 추리면 —
> **패키지**=Gradle 모듈, **판별 유니온**=`sealed interface`+`record`(`enum`이 아니다),
> `Map`은 **객체 키를 참조로 비교한다**(Java의 `HashMap`과 다르다).

## 이미 결정된 것 — 런타임

**팀 결정(2026-08-27):** 프로젝트 언어와 프레임워크는 **TypeScript + Electron**이다.
Rust·Tauri 프로토타입은 이 결정에 따라 전부 이관했다.

**팀 결정:** 패키지는 **feature 단위 상위 패키지**를 두고, 그 안을 아키텍처별로 나눈다.
`packages/pet-meta/src/{domain,ports,persistence,view}` 형태다.

이 결정으로 사라진 안건과 남은 안건이 아래에 정리돼 있다.

## 왜 지금 정해야 하나

아래 넷은 전부 **다섯 도메인에 똑같이 걸치는** 것들이다. 각자 프로토타입에서 알아서
처리하고 나중에 합치면, 다섯 가지 방식이 섞인 채로 통합하게 된다. 통합 시점에 바꾸는
비용이 지금 정하는 비용보다 훨씬 크다.

반대로 **도메인 안쪽 설계는 각자 자유로워야 한다.** 여기 없는 것은 각자 판단할 몫이다.

---

## 1. 저장소 — 기술은 정해졌고, 모양이 남았다

**팀 결정(2026-08-26):** 별도 서버와 DB를 두지 않고 **사용자 기기에 로컬 저장**한다.

이 결정으로 사라진 질문과 남은 질문을 나눠야 한다.

| 정해진 것               | 아직 안 정해진 것              |
| ----------------------- | ------------------------------ |
| 서버·네트워크·계정 없음 | 로컬에 **어떤 단위로** 쓰는가  |
| DB 서버 없음            | 마이그레이션을 누가 소유하는가 |
|                         | SQLite가 "DB"에 포함되는가     |
|                         | 저장 실패 시 무엇을 보여주는가 |

### 먼저 짚을 것 — "메모리"는 프로세스 메모리가 아니다

논의에서 "사용자의 메모리를 쓴다"는 표현이 나왔는데, 이것이 **앱을 끄면 다 사라진다**는
뜻이라면 기획서 여섯 곳과 충돌한다.

| 기획서  | 요구                                                                 |
| ------- | -------------------------------------------------------------------- |
| 8.2     | "재스캔과 앱 **재실행**은 정상 기준점을 과거로 되돌리지 않는다"      |
| 4.3     | "이미 기준점이 있는 **재실행**은 최초 실행으로 취급하지 않는다"      |
| 5.1     | 조련사 이름은 "생성 결과를 **저장하며** 실행할 때마다 바꾸지 않는다" |
| SET-007 | "오버레이 숨김 상태가 **재실행 후 유지**되고"                        |
| 9.4     | "설치 이후 이벤트에서 사실을 **영속적으로** 투영한다"                |
| ACH-004 | 새 업적을 설치 이후 사실로 소급 판정                                 |

제품으로서도 성립하지 않는다. 타마고치인데 앱을 끄면 펫이 사라지고, 매 실행이 "첫 스캔"이라
**사용량이 영원히 0이고 코인이 쌓이지 않는다.** 그래서 "메모리에서 돌리되 로컬에 저장한다"로
읽었고, `meta` 프로토타입은 그렇게 구현했다.

### `meta`가 채택한 모양

메모리를 런타임 표현으로 쓰고, 변경 뒤에 로컬 파일로 스냅샷을 남긴다.

```ts
export interface MetaStore {
  load(): MetaSnapshot | undefined; // 없으면 undefined = 새 설치
  save(snapshot: MetaSnapshot): void;
}
```

Java로 치면 `Repository` 인터페이스 하나다. 도메인은 "저장한다 / 읽어 온다"만 알고,
파일인지 SQLite인지는 앱이 정한다. 테스트는 인메모리 구현을 꽂는다.

**저장 형식과 런타임 표현을 분리했다.** 엔티티와 DTO를 나누는 것과 같다.

- `MetaState`는 조회에 맞춘 모양이다. `Map`과 `Set`을 쓴다.
- 그런데 `JSON.stringify(new Map())`은 `{}`를 준다. **`Map`과 `Set`은 JSON에 실리지
  않는다.** (테스트로 고정해 두었다)
- 게다가 `JSON.stringify`는 값이 `undefined`인 프로퍼티를 **통째로 지운다.** 그래서 저장
  형식은 "없음"을 `null`로 명시한다. 도메인 타입을 그대로 저장 형식으로 재사용하면 이게
  샌다 — 실제로 한 번 물렸다.
- `MetaSnapshot`은 맵을 배열로 펴고 `null`을 쓰는 형식이고, 각 항목이 자기 키를 필드로
  들고 있어 파일만 열어 봐도 읽힌다.

분리해 두면 런타임 자료구조를 바꿔도 저장 파일이 안 깨지고, 저장 형식을 바꿀 때는
`schema_version`을 올려 한곳에서 변환한다.

**쓰기는 원자적이어야 한다.** 파일을 직접 덮어쓰면 저장 도중 앱이 죽었을 때 반쪽짜리
JSON이 남고, 다음 실행에서 파싱이 실패해 **사용자가 펫과 업적을 통째로 잃는다.** 임시
파일에 완전히 쓴 뒤 `rename`으로 갈아 끼우면 중간 상태가 존재하지 않는다.

깨진 파일은 조용히 지우지 않고 `.corrupt-<시각>`으로 옮겨 둔다. 복구 여지와 버그 리포트
첨부를 위해서다.

### 남은 안건 — 저장 단위

**이것이 다섯 명이 정해야 할 핵심이다.**

| 선택지                                                      | 결과                                                                                                                                                               |
| ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **A. 도메인마다 파일 하나** (`meta.json`, `battle.json`, …) | 소유권이 선명하고 도메인이 독립적이다. 한 파일이 깨져도 나머지는 산다. 대신 **도메인 간 원자적 변경이 불가능하다** — "펫 획득 + 코인 차감"이 반쪽만 저장될 수 있다 |
| **B. 공용 파일 하나**                                       | 한 번의 쓰기로 전부 원자적이다. 대신 저장 포맷을 다섯 명이 함께 관리하고, 한 도메인의 버그가 전체 파일을 오염시킨다                                                |
| **C. SQLite 파일 하나**                                     | 트랜잭션과 마이그레이션을 공짜로 얻는다. 서버가 아니라 파일이라 "DB 서버 없음" 결정과 충돌하지 않을 수 있다. 대신 의존성이 늘고 스키마를 함께 설계해야 한다        |

C가 "DB 안 쓴다"에 걸리는지부터 확인해야 한다. **SQLite는 서버가 아니라 라이브러리이고
파일 하나다.** 결정의 의도가 "서버를 안 둔다"였다면 C는 후보로 남는다.

### 결정해야 할 나머지

- **원자성이 필요한 도메인 간 변경이 실제로 있는가.** 없다면 A가 가장 싸다.
  기획서 9.5의 `grant_once`는 멱등 키로 재시도하게 되어 있어 원자성 없이도 성립한다
- **스키마 버전을 지금부터 넣는가.** `meta`는 이미 넣었다. 나중에 넣으면
  "버전 없는 파일"을 위한 특수 처리가 영구히 남는다
- **저장 실패 시 UX.** 기획서 11.1 오류 표에 **저장 실패 항목이 없다.**
  디스크가 꽉 찼거나 권한이 없을 때 무엇을 보여줄지 기획에 추가해야 한다
- **저장 시점.** `meta`는 변경 직후마다 저장한다(파일이 140KB라 1ms 미만).
  데이터가 커지는 도메인은 debounce가 필요할 수 있다

## 2. 오류 처리 규약

**현재 상태:** 이관 과정에서 방식이 하나 정해졌고, 세분 정도가 남았다.

TypeScript에는 체크 예외가 없다. Rust에서는 실패를 `Result<T, E>` 반환값으로 돌려줬는데,
TypeScript에서 그것을 흉내내면 관용에서 멀어진다. 그래서 **포트는 던지고, 화면 모델
계층이 블록마다 잡는다**로 정했다.

```ts
// ports: 실패를 던진다
overlayPet(): PetSummary;  // 실패하면 PortError

// view: 블록마다 잡아 Field<T>로 바꾼다
export function fieldOf<T>(read: () => T): Field<T> {
  try {
    return { value: read(), error: undefined };
  } catch (error) {
    return { value: undefined, error: String(error) };
  }
}
```

기획서 11.1·INFO-007이 요구하는 "다른 도메인 조회 오류는 해당 블록만 오류 처리"가
`try`의 **위치**로 표현된다. 화면 전체를 한 `try`로 감싸면 `battle` 조회가 실패한 순간
사용량 수치까지 사라진다.

**현재 오류 타입:**

| 타입            | 위치        | Java로 치면                   |
| --------------- | ----------- | ----------------------------- |
| `PortError`     | `@pet/core` | 모듈 경계의 공통 예외         |
| `CollectError`  | `@pet/meta` | 분류된 예외 + `userMessage()` |
| `NameError`     | `@pet/meta` | 위와 같음                     |
| `CatalogError`  | `@pet/meta` | 메시지만 있는 예외            |
| `SnapshotError` | `@pet/meta` | 사용자 문구를 따로 든 예외    |

기획서 11.1은 "사용자에게는 원본 로그 내용과 내부 명령 출력 대신 도구 이름, 오류 종류,
마지막 정상 집계 시각, 재시도 행동만 보여준다"고 정한다. 이건 **오류가 분류돼 있어야**
지킬 수 있다. 자유 문자열이면 내부 정보가 새어 나간다.

| 선택지                                                         | 결과                                                                      |
| -------------------------------------------------------------- | ------------------------------------------------------------------------- |
| **A. 도메인마다 `Error` 하위 클래스 + `userMessage()`** ← 현재 | 분류가 강제되고 사용자 문구가 한곳에 모인다. 대신 클래스가 늘어난다       |
| **B. 공통 `AppError`에 코드 필드**                             | 타입이 하나로 줄고 직렬화가 쉽다. 대신 `instanceof`로 갈래를 나눌 수 없다 |
| **C. 도메인마다 자유, 포트 경계에서만 `PortError`로 변환**     | 도메인 안쪽이 자유롭다. 대신 변환 코드가 경계마다 반복된다                |

**결정해야 할 것**

- 사용자 표시 문자열의 소유자 (도메인 / 화면 모델 계층)
- 재시도 가능 여부를 타입에 담을 것인가
- IPC 경계를 넘을 때 오류를 어떤 모양으로 직렬화할 것인가
  (Electron IPC는 `Error`를 넘기면 메시지만 남고 클래스가 사라진다 — 이건 실제 제약이다)

## 3. 앱이 도메인을 끼우는 방식

**현재 상태:** 하드코딩이다. `composition.ts`의 `AppState`에 필드가 이름으로 박혀 있고,
`ipc.ts`에 IPC 핸들러 30개가 한 파일에 있다. 채널 이름은 이관하면서 `<도메인>:<동작>`
(`info:summary`, `collect:rescan`)으로 정리했지만, 그게 팀 규칙인지는 아직 합의가 없다.

Java로 치면 **`@Configuration` 클래스 하나에 모든 `@Bean`이 박혀 있고, `@RestController`
하나에 엔드포인트 30개가 몰려 있는 상태**다. IPC 핸들러는 렌더러가 부르는 진입점이라
REST 엔드포인트와 역할이 같다.

Electron에는 한 가지가 더 있다. **`preload`가 채널 목록을 확정한다.** 렌더러는 preload가
노출한 함수만 부를 수 있고 임의 채널을 못 부른다. 그래서 새 feature가 IPC를 추가하려면
`ipc.ts`와 `preload.cjs` **두 곳**을 고쳐야 한다 — 이 이중 등록을 어떻게 다룰지가 안건이다.

**왜 안건인가:** 다섯 도메인이 각자 명령을 20개씩만 만들어도 한 파일에 100개가 쌓인다.
`AppState`는 필드가 계속 늘고, 누가 무엇을 소유하는지 코드에서 안 보인다.

| 선택지                                                                       | 결과                                                                                                                                                               |
| ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **A. feature마다 `FeatureModule` 인터페이스 구현** (`handlers()`, `state()`) | Spring Boot 자동 설정처럼 feature가 자기 상태와 핸들러를 들고 다니고 앱은 등록만 한다. 대신 인터페이스 설계 비용이 있고 preload 노출을 자동화할 방법을 정해야 한다 |
| **B. 파일만 도메인별로 분리하고 등록은 수동**                                | 컨트롤러를 패키지별로 쪼개되 설정은 손으로 쓰는 쪽. 지금 구조에서 가장 싸다. 대신 등록 누락이 조용히 생기고 `AppState`는 계속 커진다                               |
| **C. feature마다 별도 preload 번들**                                         | 도메인마다 별도 스타터를 만드는 쪽. 격리가 가장 강하다. 대신 Electron은 창당 preload가 하나라서 합성 방법을 따로 만들어야 한다                                     |

**결정해야 할 것**

- IPC 채널 이름 규칙 (`<도메인>:<동작>`이 이관 중 쓰였다 — 이대로 갈지)
- 화면 모델 응답의 공통 봉투가 필요한가
  (`meta`는 `Field<T> { value, error }`로 블록별 오류를 표현 중이다. REST API에서 공통
  응답 래퍼를 둘지 말지와 같은 논의이고, 다른 도메인도 같은 게 필요할 것이다)
- 도메인 간 화면 전환을 누가 중재하는가
  (예: 업적 알림을 누르면 업적 화면, 레벨업 알림을 누르면 펫 화면)

---

## 4. 프론트엔드 공통 베이스

**현재 상태:** 사실상 없다. `renderer/app.js` 900줄 단일 파일에 `meta` 화면 전부가 들어
있고, 프레임워크는 쓰지 않았다. CSS 토큰은 정의돼 있지만 다른 feature가 가져다 쓸 구조가
아니다.

**왜 안건인가:** 프레임워크를 안 쓴 것은 **팀 선택을 열어 두려는 의도**였지 좋은 구조라서가
아니다. 다섯 feature의 화면이 붙으면 지금 방식은 무너진다. Electron으로 왔으므로 선택의
폭은 오히려 넓어졌다 — 번들러를 붙이는 비용이 Tauri 때보다 작다.

| 선택지                                     | 결과                                                                                                      |
| ------------------------------------------ | --------------------------------------------------------------------------------------------------------- |
| **A. 바닐라 유지 + 공통 모듈 추출**        | 번들러가 없어 가볍고 지금 그대로다. 대신 상태 관리와 컴포넌트 재사용을 직접 만들게 된다                   |
| **B. React / Svelte 도입**                 | 컴포넌트·상태 관리가 해결되고 Electron 생태계에서 가장 흔한 길이다. 대신 번들러가 워크스페이스에 들어온다 |
| **C. 도메인마다 자유, 디자인 토큰만 공유** | 각자 빠르게 만든다. 대신 한 화면 안에서 스타일이 섞이고 번들이 커진다                                     |

**결정해야 할 것**

- 디자인 토큰(색·간격·타이포)의 소유자와 배포 방식
- 패널 규약(400×462, 한 번에 하나)을 다른 도메인 화면도 따르는가
- 픽셀 아트 렌더링 규칙을 공유 모듈로 뺄 것인가
  (에셋 가이드의 슬라이싱·정수 배율 규칙은 지금 `renderer/pet.js`에만 있다. 도감·전투
  화면도 같은 스프라이트를 그려야 하므로 세 번 구현될 위험이 있다)

---

## 5. 이미 정해진 것으로 볼 수 있는 것

프로토타입이 채택했고, 큰 이견이 없다면 그대로 가져가도 될 것들이다.
반대 의견이 있으면 이것도 안건이다.

| 항목           | 프로토타입의 선택                                     | Java로 치면                           | 근거                                         |
| -------------- | ----------------------------------------------------- | ------------------------------------- | -------------------------------------------- |
| 패키지 분리    | feature당 패키지 1개 + 공용 커널                      | feature당 Gradle 모듈 1개 + core 모듈 | 의존 방향을 빌드가 강제                      |
| 포트 소유권    | **필요로 하는 쪽이 소유**                             | 인터페이스를 쓰는 쪽 모듈에 선언      | 공용 커널이 쓰레기통이 되는 것을 막음        |
| 공용 커널 범위 | 값 타입 · 이벤트 · `EventBus` · `PortError` · `Clock` | core 모듈에 값 객체와 이벤트만        | 로직 없음. 셋째 인터페이스가 추가되면 재검토 |
| 버전 관리      | 루트 `devDependencies` 단일화                         | 버전 카탈로그                         | 패키지마다 다른 버전이 섞이지 않음           |
| 시간           | `Clock` 인터페이스                                    | `java.time.Clock` 주입                | 시간 의존 규칙을 창 없이 테스트              |
| 이벤트 계약    | 봉투 + 판별 유니온 + `assertNever`                    | `sealed interface` + `record`         | 처리하지 않고 지나가는 이벤트가 생길 수 없음 |

### 다만 이벤트 유니온은 한 번 짚어야 한다

먼저 용어부터. `EventPayload`는 TypeScript의 **판별 유니온**인데, 이건 **`enum`이 아니다.**
Java 17의 `sealed interface`에 `record`를 붙인 것에 해당한다.

```java
// Java 17 — 지금 @pet/core에 있는 것과 같은 모양
sealed interface EventPayload
    permits PetAcquired, FusionCompleted, PetLevelup, /* … 8종 */ {}

record PetAcquired(PetId petId, Rarity rarity, AcquireSource source) implements EventPayload {}
record BattleFinished(String battleId, BattleResult result, int streak) implements EventPayload {}
```

유니온이므로 **어떤 갈래가 있는지 컴파일러가 전부 안다.** `switch`의 `default`에서
`assertNever`를 부르면 하나라도 빠뜨렸을 때 타입 오류가 난다. 새 이벤트를 추가했는데 어딘가에서 처리를 빠뜨리는 사고가
원천적으로 막힌다. 장점은 명확하다.

**대가는 유니온 목록이다.** 팀원이 자기 이벤트를 추가하려면 공용 패키지의
`EventPayload`를 고쳐야 한다. 다섯 명이 병렬로 작업하면 같은 파일에서 충돌하고,
무관한 도메인까지 전부 다시 컴파일된다.

| 선택지                                         | Java로 치면                                   | 결과                                                                                 |
| ---------------------------------------------- | --------------------------------------------- | ------------------------------------------------------------------------------------ |
| **A. 닫힌 유니온 유지** ← 현재                 | `sealed interface` 유지                       | 누락 불가능. 대신 커널이 자주 바뀌고 충돌 지점이 생긴다                              |
| **B. 타입 태그 + JSON 페이로드**               | `record Event(String type, JsonNode payload)` | 도메인이 자기 이벤트를 자기 모듈에 정의한다. 대신 컴파일러가 누락을 못 잡는다        |
| **C. 봉투는 커널, 페이로드 타입은 각 feature** | `sealed`을 떼고 열린 인터페이스로             | 절충. 대신 커널이 feature를 의존하게 돼 방향이 뒤집히거나, 컴파일 시점 검사를 잃는다 |

기획서 9.2가 이벤트 8종을 **고정 계약**으로 정하고 있어 A가 그 의도에 가장 가깝다.
다만 MVP 이후 이벤트가 늘어날 것을 감안하면 지금 합의해 두는 편이 낫다.

---

## 회의에서 이 순서로 보면 좋겠다

1. **이벤트 유니온** (5분) — 나머지 논의의 전제가 된다
2. **저장소 계약** (가장 오래) — 되돌리기 가장 비싸다
3. **오류 규약** (10분) — 저장소 결정에 딸려 온다
4. **앱 조립 방식** (10분) — 위 셋이 정해지면 거의 따라온다
5. **프론트엔드** (별도로 빼도 됨) — 백엔드 구조와 독립적으로 정할 수 있다
